#### 禁用模块, 卸载模块
```shell
MOD_NAME=algif_aead # 模块名

# 卸载模块
sudo rmmod ${MOD_NAME}

# 防止加载，还是能手动启动。
echo "blacklist ${MOD_NAME} /bin/false" | sudo tee /etc/modprobe.d/disable-${MOD_NAME}.conf

# 彻底禁用，无法启动。
echo "install ${MOD_NAME} /bin/false" | sudo tee /etc/modprobe.d/disable-${MOD_NAME}.conf
```